<footer class="bg-gray-800 py-10 mt-8">
    <div class="max-w-2xl mx-auto">
        <h2 class="text-2xl text-white font-semibold text-center mb-5">DEJA TU COMENTARIO</h2>
        <form action="" class="border border-slate-600 p-6 rounded-lg shadow-2xl">
            <div class="mb-4">
                <label for="name" class="block text-white font-bold mb-2">Nombre</label>
                <input type="text" id="name" class="w-full p-3 border border-gray-300 rounded-lg" placeholder="Nombre">
            </div>
            <div class="mb-4">
                <label for="review" class="block text-white font-bold mb-2">Reseña</label>
                <textarea id="review" class="w-full p-3 border border-gray-300 rounded-lg" placeholder="Reseña"></textarea>
            </div>
            <div class="mb-4">
                <label for="rate" class="block text-white font-bold mb-2">Valora tu experiencia</label>
                <input type="number" id="rate" class="w-full p-3 border border-gray-300 rounded-lg" placeholder="Valoración">
            </div>
            <div class="text-center">
                <button type="submit" class="border border-[#fac564] bg-[#fac564] hover:bg-slate-700 text-black  hover:text-[#fac564] transition py-2 px-4 rounded">
                    Enviar
                </button> 
            </div>
        </form>
    </div>
    <p class="text-center text-white mt-10">&copy; <span id="year"></span> Todos los derechos reservados</p>
    <script>
        const yearElement = document.getElementById('year');
        if (yearElement) {
            yearElement.textContent = new Date().getFullYear().toString();
        }
    </script>
</footer>